let userNames: string [] = ['admin', 'Bashir', 'Hamza', 'Taha', 'Shajeeha']; 
if(userNames.length === 0){
    console.log('We need to some users!');
}else{
    userNames =[];
    console.log('All user names have been removed. ' + userNames.length);
}

