// Crreating Array
let petAnimals: string[]=['Cat', 'dog', 'rabit'];
// Using for loop
for (let petAnimal of petAnimals){
    console.log(`A ${petAnimal} would make a great pet.`);
}

// A message outside of for loop
console.log('Any of these animals would make a great pet!');